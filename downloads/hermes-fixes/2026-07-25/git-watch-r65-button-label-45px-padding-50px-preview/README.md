# GIT WATCH R65 — 45px labels with 50px horizontal button padding

Review-only images derived from immutable GIT WATCH R64 renderer commit `6101afae09a28928e1c44418a717cb979e06a09b` and the superseding 45px-label review contract.

No renderer, API, installer, package, or installed runtime is changed by these mockups.

## Sole change from the prior 45px-label review

- Button horizontal padding: `28px` → `50px` per side.

## Frozen visible contracts

- Popup panel width and height: unchanged from R64 for every family.
- Button-label font size: `45px`.
- Button height: `76px`.
- Button internal vertical centering: unchanged from the prior 45px-label review.
- Button gap: `24px`.
- Title/message typography, semantic colors, panel border/radius, and message/action right-edge alignment: unchanged.
- All title, message, and button-label strings remain on one line without clipping or panel overflow.

See `measurements.json` for exact dimensions.
