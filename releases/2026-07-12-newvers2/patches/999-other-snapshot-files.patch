diff --git a/apps/desktop/src/lib/storage.ts b/apps/desktop/src/lib/storage.ts
index 6bc6ef9a..344f127f 100644
--- a/apps/desktop/src/lib/storage.ts
+++ b/apps/desktop/src/lib/storage.ts
@@ -66,6 +66,22 @@ export function persistBoolean(key: string, value: boolean) {
   writeKey(key, String(value))
 }
 
+
+export function storedNumber(key: string, fallback: number): number {
+  const value = readKey(key)
+
+  if (value === null) {
+    return fallback
+  }
+
+  const parsed = Number(value)
+  return Number.isFinite(parsed) ? parsed : fallback
+}
+
+export function persistNumber(key: string, value: number) {
+  writeKey(key, String(value))
+}
+
 export function storedString(key: string): null | string {
   return readKey(key)
 }
