# BRIEFS-STOCKS V61 — restored top portfolio comparison and metadata

Authoritative reference: the user's July 16 screenshot showing the populated dashboard-owned `Portfolio Position Comparison` above the quote rows.

## Cumulative Stock scope

- yellow human-readable date pill;
- exact undated `Stock Brief` title;
- metadata exactly `Generated end-of-day regular-market snapshot after the U.S. close · USD`;
- centered parent-toolbar `‹ DATE ›` navigation;
- HTML, CSV, and Markdown exports in the upper toolbar;
- exact preserved V52 dashboard-owned top Portfolio Position Comparison (10 purchase lots, calculated totals);
- seven Stock quote rows beneath it;
- producer-owned bottom Portfolio comparison removed;
- no Stock audio player.

The table renderer, lots, sorting, calculations, labels, and totals come from the preserved last-working V52 implementation. The installer accepts only the exact installed V60 source or exact V61 candidate, snapshots source and `web_dist`, runs 41 focused tests, typecheck, production build, verifies emitted markers, restarts the isolated preview, and rolls back on failure. Installation success remains separate from final user screenshot acceptance.
