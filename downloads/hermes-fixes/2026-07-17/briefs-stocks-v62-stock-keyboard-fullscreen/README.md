# BRIEFS-STOCKS V62 — keyboard dates, fullscreen, metadata

Cumulative additions on the accepted V61 Stock dashboard:

- `ArrowLeft` loads the newer cron date; `ArrowRight` loads the older cron date.
- Shortcuts ignore text-editing controls.
- A cyan maximize icon immediately beside `Stock Brief` toggles native fullscreen.
- Escape exits fullscreen through normal browser behavior, verified in a live browser.
- Metadata is now `Snapshot generated end-of-day after U.S. close · $USD`.
- The accepted top Portfolio Position Comparison, seven Stock rows, yellow date pill, export toolbar, and player-free layout remain intact.

The installer accepts only exact V61 or already-installed V62 source/page hashes, backs up touched files and the built bundle, runs 43 focused tests plus the complete test suite, typechecks, builds, restarts only isolated preview 9120, and verifies the live asset. Production 9119 and the Gateway are untouched.
